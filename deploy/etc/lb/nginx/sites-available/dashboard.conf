server {
    listen 30000;
    location /api {
        api;
    }
    location = /dashboard.html {
        root   /usr/share/nginx/html;
    }
    # location /swagger-ui {
    #     root   /usr/share/nginx/html;
    # }
}
